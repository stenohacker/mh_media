# Background workflow and annotation viewer repair — September 14, 2026

Scope: the live V2 builder and its new exports, plus video-popup sizing in the two existing dated exports and the website's Appearance demo copy. The user explicitly identified V2 as authoritative and said the older slide-image workflow is obsolete. No V1 builder changes, publishing, project replacement, or new HTML-demo annotation feature.

The supplied 0913background.png is 6806×3522. The old default used cover inside a 1500×900 stage and drew a second stage border, cropping the side margins into an apparent boxed white header. New background slides now use the artwork's proportions and contain the complete background. The redundant stage border is removed. Empty default-background slides are recognized on load; already authored legacy slides keep their coordinate system and saved geometry. The backgroundLayout field preserves the new layout after annotations are added, save/reload, duplication and export.

B1 adds a regular slide with the background; B2 offers Replace Background and Apply Default Background. B3 adds a scrolling slide with the same background; B4 offers the same replacement actions. New scrolling slides keep the background stationary and scroll independent annotations above it. Their content length extends to include objects below the initial viewport. Existing legacy tall-image slides retain their saved behavior. Background replacement does not reset annotations/hotspots. Drafts remain independent image annotations.

The annotation video popup uses the Reporter Tools outer 5:3 viewer geometry, with a 1440px maximum, viewport-aware margins, and video contained inside its available space. Pause, replay, timeline and close controls remain visible. This runtime/CSS was also patched in place into the two dated exports and website Appearance demo, preserving their exact embedded project-state text.

Verification:
- Real browser clicks in isolated builder: Add Regular, default replacement, Add Scrolling, its default replacement, scroll down with stationary background, and video select/open/pause/replay/close.
- Actual browser Preview serialization for Tutorial and Demo modes; regular and scrolling slide navigation. Both contain complete 6806×3522 background and no extra stage border.
- Standalone files generated by the real buildExportHtml serializer, tested in 375/768/1200px frames. Tutorial width and document scroll width both 375px. All navigation and video controls fit. Viewer widths measured 363.79, 737.28 and 1151.99px respectively; resizing preserved paused currentTime exactly. Desktop 1910px viewport: viewer 1440×864.
- Existing dated tutorial and demo: real video annotation opened at 1440×864, pause changed actual playback to paused. Website copy contains no video annotations in its saved seven-slide content, so its identical popup runtime was checked with the shared regression test; the actual Reporter Tools embed was reopened and main playback pause/seek checked.
- ordinary-slides, background-workflow, bounds, cards-drafts-popup, reporter-embed-pause and demo-pause regression checks pass. Website release checklist: 340 checks pass.
- All four modified HTML files retained exact embedded project-state. The original 11-slide working tutorial content remained equal to the backup. User edits in a separate project continued during this repair; no repair script writes the project database.
- Local server response is byte-for-byte the updated V2 source.

Activation: Chrome blocked automated reload/inspection because another extension panel was open. User told to close that panel and refresh the builder once. No unsaved user text was discarded.

Backup: mh_media/backups/before-background-workflow-20260914. Temporary browser test files were removed after verification; the regression test can regenerate its two fixture exports with --write-browser-fixtures.
