--- backups/2026-09-21-before-purple-surround-and-edge-placement/tutorial-demo-builder-v2.html
+++ tutorial-demo-builder-v2.html
@@ -24,6 +24,7 @@
       --pink: #ff90e7;
       --green: #26ab9d;
       --purple: #91a8ed;
+      --tutorial-page-gradient: linear-gradient(180deg, #ebe9fb 0%, #e8e6fb 42%, var(--purple) 100%);
       --browser-purple-gradient: linear-gradient(
         180deg,
         #f7f5ff 0%,
@@ -6587,12 +6588,13 @@
     /* Approved tutorial framing: white page, lavender surround, white frame,
        inner canvas, and navigation bounded by the same frame. */
     .tutorial-layout-viewport { position: relative; margin-inline: auto; }
+    body[data-mode="builder"] .tutorial-layout-viewport { background: var(--tutorial-page-gradient); }
     body[data-mode] .tutorial-shell.framed-tutorial {
       position: absolute; inset: 0 auto auto 0;
       width: 1616px; height: auto; max-width: none; min-height: 0;
-      display: block; margin: 0; padding: 30px 52px 0;
-      border: 2px solid #000;
-      background: linear-gradient(180deg, #f7f5ff 0%, #e3e6fc 44%, #b7c7f4 100%);
+      display: block; margin: 0; padding: 24px 54px;
+      border: 0;
+      background: transparent;
       transform: scale(var(--tutorial-frame-scale, 1)); transform-origin: 0 0;
       overflow: visible; box-shadow: none;
     }
@@ -6642,7 +6644,7 @@
     }
     body[data-mode="player"][data-tutorial-viewer] .builder-app {
       position: relative; inset: auto; width: 100%; height: auto;
-      min-height: 100dvh; display: block; overflow: visible; background: #fff;
+      min-height: 100dvh; display: block; overflow: visible; background: var(--tutorial-page-gradient);
     }
     body[data-mode="player"][data-tutorial-viewer="open"] { overflow: hidden; }
     body[data-mode="player"] .framed-tutorial .stage-wrap { overflow: hidden; }
@@ -10793,11 +10795,11 @@
         const headerHeight = header?.getBoundingClientRect().height || 0;
         const viewportWidth = document.documentElement.clientWidth || innerWidth;
         const viewportHeight = document.documentElement.clientHeight || innerHeight;
-        const pageGap = Math.max(12, Math.min(32, viewportWidth * .03));
+        const pageGap = 0; // The purple page extends to the viewport edges; the frame supplies its own inset.
         const toolbarGap = MODE === "builder" ? 28 : 0;
         const sceneHeight = slideSceneHeight();
         const designWidth = 1616;
-        const designHeight = sceneHeight + 146; // surround 30, borders 12, nav 104
+        const designHeight = sceneHeight + 160; // purple top/bottom 48, frame borders 8, nav 104
         const scale = Math.max(.01, Math.min(
           (viewportWidth - 2 * pageGap) / designWidth,
           (viewportHeight - 2 * pageGap) / designHeight
@@ -10814,7 +10816,7 @@
         // Builder and export at the same viewport must have identical widths.
         // Header removal moves the same-sized frame upward after Start. The
         // opening cover may extend below the fold while the header is visible.
-        app.style.paddingTop = `${pageGap + (MODE === "player" ? headerHeight : 0)}px`;
+        app.style.paddingTop = `${MODE === "player" ? headerHeight : 24}px`;
         app.style.paddingBottom = `${pageGap}px`;
         requestAnimationFrame(syncSelectionControls);
       });
@@ -11810,16 +11812,21 @@
             if (['image', 'shape', 'gif'].includes(item?.type)) {
               const alphaBounds = visibleImageAlphaBounds(img);
               if (alphaBounds) {
-                annotationAlphaBoundsById.set(item.id, alphaBounds);
                 const imageWidth = width;
                 const imageHeight = height;
                 left += imageWidth * alphaBounds.left;
                 top += imageHeight * alphaBounds.top;
                 width = imageWidth * (alphaBounds.right - alphaBounds.left);
                 height = imageHeight * (alphaBounds.bottom - alphaBounds.top);
-              } else {
-                annotationAlphaBoundsById.delete(item.id);
               }
+              // Bounds are relative to the placed frame, including contain-fit
+              // letterboxing. Transparent file margins and empty frame space
+              // must not stop visible artwork from reaching the canvas edge.
+              annotationAlphaBoundsById.set(item.id, {
+                left: left / object.offsetWidth, top: top / object.offsetHeight,
+                right: (left + width) / object.offsetWidth,
+                bottom: (top + height) / object.offsetHeight
+              });
             }
           }
         }
@@ -14690,6 +14697,7 @@
       if (!item || item.locked) return;
       if (kind === "annotation" && item.type === "text" && !event.target.closest("[data-handle]")) return;
       objectElement.classList.add("is-selected");
+      syncSelectionControls(); // Resolve visible image bounds before the first drag.
       const stage = objectElement.closest("[data-stage]");
       const rect = stage.getBoundingClientRect();
       drag = {
